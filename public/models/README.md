# 3D Models

Save future 3D model assets in this folder.

Recommended formats:
- `.glb` for single-file web models
- `.gltf` with related texture/bin files in a matching subfolder

Files placed here can be referenced from the site with paths like:

```text
/models/example.glb
```
